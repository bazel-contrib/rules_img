load("@bazel_skylib//rules:build_test.bzl", "build_test")
load("@rules_go//go:def.bzl", "go_binary", "go_library")

go_library(
    name = "bes_lib",
    srcs = ["bes.go"],
    importpath = "github.com/bazel-contrib/rules_img/img_tool/cmd/bes",
    visibility = ["//visibility:private"],
    deps = [
        "//pkg/auth/credential",
        "//pkg/auth/protohelper",
        "//pkg/cas",
        "//pkg/proto/build_event_service",
        "//pkg/serve/bes",
        "//pkg/serve/bes/syncer",
        "@org_golang_google_grpc//:grpc",
    ],
)

version_x_defs = {
    "github.com/bazel-contrib/rules_img/img_tool/pkg/version.Version": module_version() or "dev",
}

go_binary(
    name = "bes",
    embed = [":bes_lib"],
    visibility = ["//visibility:public"],
    x_defs = version_x_defs,
)

build_test(
    name = "test",
    targets = [":bes"],
)
